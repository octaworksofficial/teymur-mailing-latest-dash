# Railway Nixpacks configuration
[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"