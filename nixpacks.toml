# Railway Nixpacks configuration
[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[phases.install]
cmds = ["npm install --production=false"]

[start]
cmd = "NODE_ENV=production node server/production.js"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"